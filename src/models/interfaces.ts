export interface CountryObj {
    name: string;
    code: string;
}

export interface RegionObj {
    region: string;
    country: string;
}

export interface OverallLocationObj {
    city: string;
    country: string;
    region: string;
    latitude: string;
    longitude: string;
}